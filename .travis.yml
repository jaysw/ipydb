# travis-ci configuration
language: python
python:
  - "2.7"
install:
  - python setup.py develop
  - pip install nose coverage flake8 --use-mirrors
script:
  - nosetests --with-coverage --cover-package=ipydb
  - flake8 --exit-zero paython 
